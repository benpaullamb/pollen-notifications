{
  "name": "pollen-notifications",
  "version": "0.1.0",
  "bin": {
    "pollen-notifications": "bin/pollen-notifications.js"
  },
  "scripts": {
    "build": "echo \"The build step is not required when using JavaScript!\" && exit 0",
    "cdk": "cdk",
    "test": "jest"
  },
  "devDependencies": {
    "@aws-cdk/assert": "1.108.0",
    "aws-cdk": "1.108.0",
    "jest": "^26.4.2"
  },
  "dependencies": {
    "@aws-cdk/core": "1.108.0"
  }
}
